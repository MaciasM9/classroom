<p id="notice"><%= notice %></p>

<h1>Listing Projects</h1>

<table class="table  table striped table-bordered table-condensed">
  <thead style="background-color:cadetblue;color:white">
    <tr>
      <th>Project name</th>
      <th>Project description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody style="font-weight:bold">
    <% @projects.each do |project| %>
      <tr>
        <td><%= project.project_name %></td>
        <td><%= project.project_description %></td>
        <td><%= link_to project_path(project),class: 'btn btn-primary btn-lg' do %>
            <i class="glyphicon glyphicon-eye-open"></i> 
        <% end %>
        </td>
        <td><%= link_to edit_project_path(project), class: 'btn btn-warning btn-lg' do %>
            <i class="glyphicon glyphicon-pencil"></i>
        <% end %></td>
        <td><%= link_to project_path(project), method: :delete, data: { confirm: 'Are you sure?' },class: 'btn btn-danger btn-lg' do%>
          <i class="glyphicon glyphicon-trash"></i>
        <% end %>
        </td>
h
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Project', new_project_path %>
